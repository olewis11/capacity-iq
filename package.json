{
  "name": "capacity-iq",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "dev": "npx esbuild src/app.jsx --bundle --format=iife --sourcemap --outfile=dist/app.js --define:process.env.NODE_ENV='\"development\"' --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
